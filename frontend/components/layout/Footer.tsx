export function Footer() {
  return (
    <footer className="border-t border-[color:var(--border)] bg-[color:var(--surface)]/88 backdrop-blur-xl">
      <div className="mx-auto flex w-full max-w-7xl flex-col gap-2 px-4 py-6 text-sm text-[color:var(--text-muted)] md:flex-row md:items-center md:justify-between md:px-8">
        <p>AgentCart</p>
        <span>Evidence-first recommendations for supplements.</span>
      </div>
    </footer>
  );
}
